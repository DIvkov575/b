data:
  dataset: text8
  seq_len: 32
  max_samples: 500
  vocab_size: 28

model:
  hidden_dim: 64
  num_layers: 2
  num_heads: 4
  dropout: 0.0

training:
  epochs: 5
  lr: 0.001
  batch_size: 64
  schedule: uniform
  warmup_steps: 0

sampling:
  num_steps: 10
  num_samples: 50
