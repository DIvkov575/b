data:
  K: 4
  L: 8
  n_train: 1000
  n_val: 200

model:
  hidden_dim: 32
  num_layers: 2
  time_embed_dim: 32

training:
  epochs: 10
  lr: 0.001
  batch_size: 64
  flow_type: prob_path

guidance:
  gamma: 2.0
  num_steps: 20
  num_samples: 200

properties:
  - name: starts_with_0
  - name: contains_pattern
  - name: no_repeats
