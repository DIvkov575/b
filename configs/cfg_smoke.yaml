data:
  K: 4
  L: 8
  n_train: 1000
  n_val: 200

model:
  hidden_dim: 32
  num_layers: 2

training:
  epochs: 20
  lr: 0.001
  batch_size: 64
  cfg_dropout_prob: 0.15

guidance:
  w: 3.0
  num_steps: 10
  num_samples: 100

properties:
  - name: starts_with_0
  - name: contains_pattern
  - name: no_repeats
