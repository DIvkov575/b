outputs/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.env

# Local-only archive of superseded experiments + large deploy assets (see .archive/README.md)
.archive/
# Deploy assets / secrets — never commit
deploy/
*.pem

# L37: vendored third-party repo, downloaded checkpoints/data, local venvs (all reproducible
# via docs/L37_PIPELINE_SPEC.md — not source, don't commit large binaries)
third_party/mdgen/
.hf_cache/
.pilot_data/
.venv-mdgen/
.venv-mdgen311/

# L35: same pattern — vendored DiffCSP repo, downloaded checkpoint, local venv (all
# reproducible via docs/L35_PIPELINE_SPEC.md — not source, don't commit large binaries)
third_party/diffcsp/
third_party/diffcsp_checkpoints/
.venv-diffcsp/

# L38: local venv (reproducible via docs/L38_PROTOCOL.md); vendored ProteinMPNN lives
# only on the EC2 host (fetched via tarball, not committed — large binary weights)
.venv-l38/
src/l38/vendor/

# L40: downloaded Boltz MSA .npz slice + fetched RCSB baseline sequences (reproducible
# via docs/L40_PROTOCOL.md — not source, don't commit large binaries)
data/l40_pilot/

# L35 paper: LaTeX build artifacts (regenerated by pdflatex/bibtex, not source)
docs/log2026_cfp/submission/*.aux
docs/log2026_cfp/submission/*.log
docs/log2026_cfp/submission/*.out
docs/log2026_cfp/submission/*.blg
docs/log2026_cfp/submission/*.brf
